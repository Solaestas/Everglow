# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET Build and Test

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build-and-test:

    runs-on: windows-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v6

    - name: Fetch full git history
      shell: bash
      run: |
        # Convert shallow clone to full history for accurate comparison
        git fetch --prune --unshallow || true
        # Fetch latest master to perform upstream divergence check
        git fetch --prune origin master

    - name: Compare with master
      id: divergence
      shell: pwsh
      run: |
        # Perform an upstream divergence check
        $behind = git rev-list --count HEAD..origin/master
        $ahead  = git rev-list --count origin/master..HEAD

            Write-Host "Ahead commits: $ahead"
            Write-Host "Behind commits: $behind"

            echo "ahead=$ahead"   >> $env:GITHUB_OUTPUT
            echo "behind=$behind" >> $env:GITHUB_OUTPUT

            if ($behind -ne 0) {
              Write-Error "❌ This branch is behind origin/master by $behind commits"
              exit 1
            }

            Write-Host "✅ Branch is up to date with origin/master"

    - name: Auto comment on PR if behind
      if: ${{ github.event_name == 'pull_request' && steps.divergence.outputs.behind != '0' }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      shell: bash
      run: |
        gh pr comment ${{ github.event.pull_request.number }} --body "
        ⚠️ **Your branch is ${{ steps.divergence.outputs.behind }} commits behind master.**
  
    - name: Setup .NET 8.0
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x
        cache: true
        cache-dependency-path: '**/*.csproj'

    - name: Restore dependencies
      run: |
          touch .\Sources\Directory.Build.props
          echo '<Project>
                  <PropertyGroup>
                    <TargetFramework>net8.0</TargetFramework>
                    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
                    <ImplicitUsings>enable</ImplicitUsings>
                    <PlatformTarget>AnyCPU</PlatformTarget>
                    <LangVersion>preview</LangVersion>
                    <NoWarn>$(NoWarn);CS0436</NoWarn>
                    <Modules>AssetReplace;CagedDomain;EternalResolve;Example;Food;MEAC;Minortopography;Myth;Ocean;Plant;PlantAndFarm;SpellAndSkull;SubSpace;Yggdrasil</Modules>
                    <DefineConstants>$(DefineConstants);$(Modules)</DefineConstants>
                    <EnableModBuilder>false</EnableModBuilder>
                    <EnablePathGenerator>true</EnablePathGenerator>
                    <CompileEffect>true</CompileEffect>
                    <NotCopyLocal>true</NotCopyLocal>
                  </PropertyGroup>
                  <ItemGroup>
                    <AdditionalFiles Include="**\*.json" Pack="true" ModPath="%(Identity)" />
                  </ItemGroup>
                  <ItemGroup>
                    <AdditionalFiles Include="**\*.atlas" Pack="true" ModPath="%(Identity)" />
                  </ItemGroup>
                  <ItemGroup>
                    <PackageReference Include="Solaestas.tModLoader.ModBuilder" Version="1.5.11" />
                    <PackageReference Include="StyleCop.Analyzers.Unstable" Version="1.2.0.556">
                      <PrivateAssets>all</PrivateAssets>
                      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
                    </PackageReference>
                    <Publicize Include="ReLogic" />
                    <Publicize Include="MonoMod.RuntimeDetour" />
                    <Publicize Include="MonoMod.Utils" />
                    <AdditionalFiles Include="**\*.obj;**\*.bmp;**\*.mapio;**\*.ttf" Pack="true" ModPath="%(Identity)" Exclude="bin\**\*;obj\**\*" />
                    <AdditionalFiles Include="$(MSBuildThisFileFullPath)" Pack="false" Visible="false" />
                  </ItemGroup>
                </Project>' > .\Sources\Directory.Build.props
          dotnet restore

    - name: Pull TML
      run: |
          cd ..
          mkdir ./tmod
          touch tModLoader.targets

          echo '<Project ToolsVersion="14.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
              <Import Project=".\tmod\tMLMod.targets" />
          </Project>' > tModLoader.targets

          cd ./tmod
          curl -L -o tModLoader.zip https://github.com/tModLoader/tModLoader/releases/latest/download/tModLoader.zip
          # wget https://github.com/tModLoader/tModLoader/releases/latest/download/tModLoader.zip
          unzip tModLoader.zip
          cd ..
          cd ./Everglow

    - name: Download XNA Framework Redistributable
      shell: pwsh
      run: |
        Invoke-WebRequest `
          -Uri "https://download.microsoft.com/download/a/c/2/ac2c903b-e6e8-42c2-9fd7-bebac362a930/xnafx40_redist.msi" `
          -OutFile "xnafx40_redist.msi"

    - name: Install XNA Framework Redistributable
      shell: pwsh
      run: |
        Start-Process msiexec.exe -ArgumentList "/i xnafx40_redist.msi /quiet /norestart" -Wait

    - name: Build
      run: dotnet build /p:Configuration=Release /p:WarningLevel=0
      
    - name: Test
      run: dotnet test --verbosity normal /p:WarningLevel=0
